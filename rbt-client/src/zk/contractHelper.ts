// src/zk/contractHelper.ts
import { ethers } from "ethers";

declare global {
    interface Window {
      ethereum: any;
  }
}

export async function submitProof(proof: any, publicSignals: any) {
 
}
